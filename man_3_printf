./documentation about printf work
.SH NAME
_printf() - takes input and prints to standard output.

.SH Sintaxis
int _printf(const char *format, ...)


.SH DESCRIPTION
	the function _printf() sends formatted output to stdout and uses format
	specifiers to convert arguments into strings. The format specifiers are:
	FORMAT outputs a specified format relative to the specifier input.
    
.SH FORMAT
    a character is used, to call so to speak a specific type of format

    example
    $ _printf ("% C", we indicate n character);
    $ _printft "% d", we indicate a decimal number);


.SH AUTHOR
Julio Contreras and Adrian Guerra
